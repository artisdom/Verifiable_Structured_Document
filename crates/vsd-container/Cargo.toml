[package]
name = "vsd-container"
description = "Container I/O for VSD (Verifiable Structured Document): chunked .vsd file reading and writing with integrity verification."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[features]
default = ["zstd"]
zstd = ["dep:zstd"]
# Pure-Rust zstd *decompression* (no C toolchain, wasm-friendly).
# Readers work fully; writing compressed chunks still requires `zstd`.
zstd-pure = ["dep:ruzstd"]

[dependencies]
vsd-core = { workspace = true, features = ["std"] }
blake3 = { workspace = true }
thiserror = { workspace = true }
zstd = { workspace = true, optional = true }
ruzstd = { version = "0.8", optional = true }

[package.metadata.docs.rs]
all-features = true
